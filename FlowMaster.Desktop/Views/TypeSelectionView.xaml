<UserControl x:Class="FlowMaster.Desktop.Views.TypeSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600">
    
    <UserControl.Resources>
        <Style x:Key="TypeCardButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#ddd"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="30,40"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f0f7ff"/>
                                <Setter Property="BorderBrush" Value="#002c5f"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="#F6F3F2">
        <Border Background="White" CornerRadius="12" Margin="40" Padding="40"
                VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Opacity="0.1" ShadowDepth="5"/>
            </Border.Effect>
            
            <StackPanel>
                <!-- Header -->
                <TextBlock Text="📋 새 테스트 문서 생성" 
                           FontSize="24" FontWeight="Bold" Foreground="#002c5f"
                           HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <TextBlock Text="테스트 유형을 선택하세요:" 
                           FontSize="16" Foreground="#555"
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>
                
                <!-- Type Selection Cards -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <!-- BA1 Card -->
                    <Button Style="{StaticResource TypeCardButton}" 
                            Command="{Binding SelectTypeCommand}" CommandParameter="BA1">
                        <StackPanel>
                            <TextBlock Text="BA1" FontSize="32" FontWeight="Bold" 
                                       Foreground="#002c5f" HorizontalAlignment="Center"/>
                            <TextBlock Text="22항목" FontSize="14" Foreground="#666" 
                                       HorizontalAlignment="Center" Margin="0,10,0,0"/>
                            <Border Background="#ffe0e0" CornerRadius="4" Padding="8,4" Margin="0,10,0,0">
                                <TextBlock Text="Description 없음" FontSize="12" Foreground="#c00"/>
                            </Border>
                        </StackPanel>
                    </Button>
                    
                    <!-- BA2 Card -->
                    <Button Style="{StaticResource TypeCardButton}" 
                            Command="{Binding SelectTypeCommand}" CommandParameter="BA2">
                        <StackPanel>
                            <TextBlock Text="BA2" FontSize="32" FontWeight="Bold" 
                                       Foreground="#002c5f" HorizontalAlignment="Center"/>
                            <TextBlock Text="24항목" FontSize="14" Foreground="#666" 
                                       HorizontalAlignment="Center" Margin="0,10,0,0"/>
                            <Border Background="#e0ffe0" CornerRadius="4" Padding="8,4" Margin="0,10,0,0">
                                <TextBlock Text="Description 포함" FontSize="12" Foreground="#080"/>
                            </Border>
                        </StackPanel>
                    </Button>
                </StackPanel>
                
                <!-- Clone Section -->
                <Border Background="#f8f8f8" CornerRadius="8" Margin="0,30,0,0" Padding="15">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="🔄 기존 문서에서 복제:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox ItemsSource="{Binding ExistingDocuments}" 
                                  SelectedItem="{Binding SelectedCloneSource}"
                                  DisplayMemberPath="Title" Width="300"
                                  IsEnabled="{Binding IsDbConnected}"/>
                    </StackPanel>
                </Border>
                
                <!-- DB Connection -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="📂 외부 DB 연결" Command="{Binding ConnectDbCommand}"
                            Padding="15,8" Margin="5"/>
                    <TextBlock Text="{Binding DbConnectionStatus}" VerticalAlignment="Center" 
                               Foreground="#666" FontSize="12"/>
                </StackPanel>
                
                <!-- Cancel Button -->
                <Button Content="취소" Command="{Binding CancelCommand}"
                        HorizontalAlignment="Center" Margin="0,30,0,0"
                        Padding="30,10" Background="#e4dcd3" BorderThickness="0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
